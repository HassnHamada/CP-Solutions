#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

const int N = 2e3 + 20, M = 1e2 + 10, MOD = 1e9 + 7, HV = 151, INF = 0x3f3f3f3f;
char tem[M];

void run()
{
    while (~scanf("%s%s%s", tem, tem, tem))
    {
        printf("NO\n");
        fflush(stdout);
        // cout << "NO\n";
        // cout.flush();
    }
}

int main()
{
    // freopen("_input.txt", "r", stdin);
    // freopen("_output.txt", "w", stdout);
    int t = 1;
    // scanf("%d", &t);
    while (t--)
    // while (scanf("%d", &n), n)
    {
        run();
    }

    return 0;
}