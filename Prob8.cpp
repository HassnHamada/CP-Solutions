#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

const int N = 1e3 + 10, M = 5, MOD = 1e9 + 7, HV = 151, INF = 0x3f3f3f3f;

void run()
{
    // Task
    //      A group of people sat on a round table to play a game the game consists of k rounds
    //      where in each round the i's person have to pass all the coins he has to the ai person
    //      The task is to know how much coin will every one have after k round
    // Input
    //      Line 1 : n the number of points 3 <= n <= 1e3 and 0 <= k <= 1e18
    //      Line 2 : contains n intger where a1 means that the player with index 1 will pass all his coins to a player with index a1
    // Output
    //      One line containing n intger where the i'th value represents how much coin the i'th person have
}

int main()
{
    // freopen("_input.txt", "r", stdin);
    // freopen("_output.txt", "w", stdout);
    int t = 1;
    // scanf("%d", &t);
    while (t--)
    // while (scanf("%d", &n), n)
    {
        run();
    }

    return 0;
}